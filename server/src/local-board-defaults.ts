/** Default identity for the implicit `local_trusted` board operator (`local-board`). */
export const LOCAL_BOARD_USER_ID = "local-board" as const;
export const LOCAL_BOARD_USER_EMAIL = "local@ai-harness.local";
export const LOCAL_BOARD_USER_NAME = "Board";
