/**
 * When false, the main UI hides beta/alpha surfaces: Routines nav, Instance
 * Experimental & Plugins, and internal dev routes (design guide, UX lab).
 * Flip to true locally if you need those entry points.
 */
export const SHOW_BETA_UI = false;
