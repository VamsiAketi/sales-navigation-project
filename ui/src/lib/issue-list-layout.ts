/**
 * Fixed width for the status lozenge column in issue list / inbox rows (sm+).
 * Keeps ID and title left edges aligned when labels differ in length (e.g. DONE vs IN PROGRESS).
 */
export const ISSUE_LIST_STATUS_COLUMN_WIDTH_CLASS = "w-[8.5rem] shrink-0";
