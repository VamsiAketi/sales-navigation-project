DATABASE_URL=postgres://paperclip:paperclip@localhost:5432/paperclip
PORT=3100
SERVE_UI=false
# Human invites use Better Auth email sign-up internally. If `~/.paperclip/.../config.json` sets
# auth.disableSignUp: true, set explicitly: PAPERCLIP_AUTH_DISABLE_SIGN_UP=false
# PAPERCLIP_AUTH_DISABLE_SIGN_UP=false
# Outbound email (Microsoft Graph). App needs Mail.Send (application) + admin consent.
# Optional: User.Read.All or User.ReadBasic.All so the server can resolve MS_SENDER_EMAIL to an object id
# (or set MS_SENDER_OBJECT_ID to skip lookup).
# Token: POST https://login.microsoftonline.com/{MS_TENANT_ID}/oauth2/v2.0/token
# Send:  POST https://graph.microsoft.com/v1.0/users/{object-id-or-encoded-UPN}/sendMail
MS_TENANT_ID=
MS_CLIENT_ID=
MS_CLIENT_SECRET=
MS_SENDER_EMAIL=
# Optional: Entra object id of the sender mailbox (avoids sender lookup and UPN in sendMail URL).
# MS_SENDER_OBJECT_ID=
# Optional: log JWT app roles / scp on token acquisition (no secrets).
# MS_GRAPH_DEBUG=false

# Microsoft Entra / Azure AD sign-in (authenticated mode). Tenant ID is optional (defaults to multi-tenant "common").
# AI_HARNESS_AUTH_MICROSOFT_TENANT_ID=
# AI_HARNESS_AUTH_MICROSOFT_CLIENT_ID=
# AI_HARNESS_AUTH_MICROSOFT_CLIENT_SECRET=
#
# Optional: when a user signs in with Microsoft but has no company membership yet, add them automatically:
# PAPERCLIP_AUTH_MICROSOFT_AUTO_PROVISION_COMPANY_ID=<uuid>   # or use COMPANY_IDS for multiple
# PAPERCLIP_AUTH_MICROSOFT_AUTO_PROVISION_COMPANY_IDS=<uuid>,<uuid>
# PAPERCLIP_AUTH_MICROSOFT_AUTO_PROVISION_MEMBERSHIP_ROLE=member
# Optional allowlist (comma-separated domains, no @): only these email domains get auto-provisioned
# PAPERCLIP_AUTH_MICROSOFT_AUTO_PROVISION_EMAIL_DOMAINS=contoso.com,fabrikam.com

# Stripe (optional). Use a test secret key from Dashboard → Developers → API keys (Test mode).
# No real money moves in test mode; use test cards (e.g. 4242424242424242) and Stripe’s test data.
# PAPERCLIP_STRIPE_SECRET_KEY=sk_test_...
# (alias) STRIPE_SECRET_KEY=sk_test_...
# Stripe webhook signing secret from Developers → Webhooks.
# PAPERCLIP_STRIPE_WEBHOOK_SECRET=whsec_...
# (alias) STRIPE_WEBHOOK_SECRET=whsec_...

# Control Plane internal callbacks (same base URL + INTERNAL_SECRET).
# Agent run complete: {CONTROL_PLANE_URL}/api/internal/agent-run-complete
# Daily ticket closure: {CONTROL_PLANE_URL}/api/internal/ticket-closure
CONTROL_PLANE_URL=https://tenants.ai-harness.com
INTERNAL_SECRET=ai-harness-internal-secret-2026
